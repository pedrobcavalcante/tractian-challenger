class Constants {
  static const Duration splashDelay = Duration(seconds: 3);
  static const int debounceDelayMs = 300;
  static const int maxCacheSize = 20;
  static const int maxTreeSizeForSync = 1000;
  static const Duration animationDuration = Duration(milliseconds: 200);
}
